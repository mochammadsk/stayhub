/* NEXT DEVELOPMENT */

// module.exports = (app) => {
//   const midtrans = require('../handlers/midtrans.handler');
//   const router = require('express').Router();

//   // Webhook midtrans
//   router.post('/webhook', (req, res) => {
//     midtrans.webhook(req, res);
//   });

//   app.use('/', router);
// };
